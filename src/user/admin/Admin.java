package user.admin;

import user.User;

public class Admin extends User {
    Admin(String username,String password,String email,String phoneNumber){
        super(username,password,email,phoneNumber);
    }
}
